imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

image:
  repository: kralicky/opni-monitoring # Overrides the image tag whose default is the chart appVersion.
  tag: main
  pullPolicy: Always

auth:
  provider: openid
  openid:
    # Enter the URL where your openid provider serves its JWK Set
    jwkSetUrl: ""

gateway:
  replicaCount: 1
  podAnnotations: {}
  podSecurityContext: {}
  dnsNames: [] # TLS SANs to add to the gateway serving cert (required)
  securityContext:
    {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000

  resources:
    {}
    # requests:
    #   cpu: "500m"
    #   memory: "512Mi"
    # limits:
    #   cpu: "2"
    #   memory: "2Gi"

service:
  type: LoadBalancer
  port: 8080

management:
  # The management service can listen on a unix socket or a TCP port.
  # For example:
  #   unix:///path/to/management.sock
  #   tcp://0.0.0.0:9090
  listenAddress: unix:///run/opni-monitoring/management.sock

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

nodeSelector: {}

tolerations: []

affinity: {}
