apiVersion: v1
kind: Service
metadata:
  name: opni-gateway
  labels:
    app: opni-gateway
spec:
  type: LoadBalancer
  selector:
    app: opni-gateway
  ports:
    - name: http
      port: 443
      targetPort: http
