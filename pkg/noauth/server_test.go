package noauth_test

import (
	"testing"
)

func TestServer(t *testing.T) {
	// srv := noauth.NewServer("foo", "bar", "http://localhost:3000", nil)
	// if err := srv.Run(); err != nil {
	// 	t.Fatal(err)
	// }
}
