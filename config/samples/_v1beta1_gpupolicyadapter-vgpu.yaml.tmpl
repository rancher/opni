apiVersion: opni.io/v1beta1
kind: GpuPolicyAdapter
metadata:
  name: vgpu
spec:
  images:
    driver: your.registry.local/vgpu-driver:tag
  vgpu:
    licenseConfigMap: licensing-config
    licenseServerKind: nls
